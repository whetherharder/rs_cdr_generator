# Rust
/target/
**/*.rs.bk
# Cargo.lock коммитится намеренно — в изолированном контуре нет crates.io,
# и без зафиксированных версий сборка невоспроизводима.

# Output
out/
*.tar.gz
*.csv
*.json

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary
tmp/
temp/

# Вендор-каталог офлайн-сборки (Dockerfile, cargo vendor) — не коммитим,
# ~140 МБ чужого кода без диффа; генерируется командой из README перед
# сборкой образа для изолированного контура.
/vendor/

# Benchmarking and profiling results
benchmark_results/
profiling_results/
benchmark_test/
flamegraph_temp/
*.trace
perf.data
perf.data.old
*.svg
*.txt
!PROFILING.md
!README.md
